name: cimxml-ci

on:
  pull_request:
    paths:
      - "cimxml/**"
      - ".github/workflows/cimxml-ci.yml"
      - ".github/workflows/cimxml-release.yml"
  push:
    paths:
      - "cimxml/**"
      - ".github/workflows/cimxml-ci.yml"
      - ".github/workflows/cimxml-release.yml"

permissions:
  contents: read

concurrency:
  group: cimxml-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Java 21
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: cimxml/pom.xml

      - name: Build and test
        run: mvn -B -ntp -f cimxml/pom.xml clean verify

      # License-header status for cimxml (ADVISORY / non-blocking).
      # To ENFORCE it:
      #   1. mvn -f cimxml/pom.xml com.mycila:license-maven-plugin:5.0.0:format -Dcimxml.license.skip=false
      #   2. set <cimxml.license.skip> to false in cimxml/pom.xml
      #   3. delete this `continue-on-error` (the check then gates the build via `verify`)
      - name: License headers — cimxml (advisory, not yet enforced)
        continue-on-error: true
        run: mvn -B -ntp -f cimxml/pom.xml com.mycila:license-maven-plugin:5.0.0:check -Dcimxml.license.skip=false

  publish-snapshot-github-packages:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Setup Java 21 for GitHub Packages
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: cimxml/pom.xml
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - name: Compute and apply snapshot version
        shell: bash
        run: |
          set -euo pipefail
          CIMXML_VERSION="$(bash scripts/compute-version.sh cimxml)"
          echo "cimxml version: ${CIMXML_VERSION}"
          bash scripts/set-versions.sh "${CIMXML_VERSION}"

      - name: Deploy snapshot to GitHub Packages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mvn -B -ntp -f cimxml/pom.xml clean deploy \
            -DaltDeploymentRepository=github::https://maven.pkg.github.com/${{ github.repository }}
