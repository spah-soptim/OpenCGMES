# Generated at build time by Gradle (processResources expands ${pluginVersion}).
# Read by CimcheckServerConnectionProvider to key the extracted server-JAR cache,
# avoiding any IntelliJ plugin-manager API (which is internal / version-unstable).
version=${pluginVersion}
